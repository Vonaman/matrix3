<div class="absolute top-0 right-0 z-50">
    <div id="timer"
     x-data="timerComponent({{ endtime * 1000 }})" 
    x-init="start()"
    x-text="timeLeft"
    class="text-green-500 text-2xl font-bold bg-black border-2 border-green-500 rounded-bl-3xl px-4 py-3"></div>
</div>