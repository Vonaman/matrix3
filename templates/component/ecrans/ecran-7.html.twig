<div class="p-60 w-full h-full flex items-center justify-center bg-black text-white text-3xl relative">
    <p class="absolute text-[#20E820] text-lg right-24 bottom-96"
        x-data="{ 
                visible: true, 
                showTime: 1000,
                hideTime: 4000,
                toggle() {
                    this.visible = true;
                    setTimeout(() => {
                        this.visible = false;
                    }, this.showTime);
                }
            }"
        x-init="
                toggle();
                setInterval(() => { toggle() }, showTime + hideTime)
        "
        x-show="visible"
        x-transition>
            6
    </p>
    <span class="absolute w-8 h-4 rounded-full bg-blue-400 rotate-[23deg] top-6 right-96"></span>
    <div class="absolute h-8 w-8 matrix-close-btn rounded-lg p-2 top-8 left-32 flex flex-col justify-center items-center icon-svg">
    <!-- Remplace les spans par ton SVG ici -->
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" class="matrix-close-icon">
            <path d="M14 8L8 14M8 8L14 14M21 11C21 16.5228 16.5228 21 11 21C5.47715 21 1 16.5228 1 11C1 5.47715 5.47715 1 11 1C16.5228 1 21 5.47715 21 11Z" stroke="#20E820" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
    </div>

    <p class="text-[#20E820] text-[8px] absolute bottom-1 left-1">
        Un dixième écran se cache dans l'inpecteur.
    </p>
</div>